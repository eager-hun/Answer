/**
 * @file
 * Theme-agnostic scripts for the Full Page Prototype.
 */
(function ($, window, document, undefined) {
  "use strict";

  if (typeof console === "undefined") {
    this.console = { log: function() {} };
  }
  console.log('fpp script.');

})(jQuery, this, this.document);
