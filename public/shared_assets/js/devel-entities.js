/**
 * @file
 * Theme-agnostic scripts for the devel_entities feature (data handler).
 */
(function ($, window, document, undefined) {
  "use strict";

  if (typeof console === "undefined") {
    this.console = { log: function() {} };
  }
  console.log('devel-entities script.');

})(jQuery, this, this.document);
